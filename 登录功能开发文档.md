# 登录功能开发文档

## 版本信息

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-04 | 初始版本 |
| v1.1 | 2026-06-04 | 改为明文密码存储 |

---

## 一、功能概述

### 1.1 登录方式

系统支持两种登录方式：

**1. 账号密码登录**
- 用户名 + 明文密码
- 适用于：管理员、普通用户

**2. 手机号验证码登录**
- 手机号 + 短信验证码
- 验证码控制台打印（开发环境）
- 自动注册新用户

### 1.2 密码管理

- **注册**：明文存储密码
- **登录**：明文密码验证
- **修改密码**：支持首次设置和修改密码
- **密码格式**：6-20 位数字或字母

### 1.3 验证码服务

- 6 位数字验证码
- 有效期：5 分钟
- 发送间隔：60 秒
- 控制台打印（开发环境）

---

## 二、数据库设计

### 2.1 user 表结构

```sql
CREATE TABLE `user` (
    `id` BIGINT NOT NULL AUTO_INCREMENT,
    `username` VARCHAR(50) NOT NULL UNIQUE,
    `password` VARCHAR(100) NOT NULL COMMENT '明文密码',
    `nickname` VARCHAR(50) DEFAULT NULL,
    `avatar` VARCHAR(255) DEFAULT NULL,
    `phone` VARCHAR(20) DEFAULT NULL,
    `phone_verified` TINYINT DEFAULT 0,
    `role` TINYINT DEFAULT 0 COMMENT '0-普通用户 1-管理员',
    `status` TINYINT DEFAULT 1 COMMENT '0-禁用 1-正常',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `update_time` DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (`id`),
    KEY `idx_username` (`username`),
    KEY `idx_phone` (`phone`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 2.2 测试数据

```sql
-- 管理员账号
INSERT INTO `user` VALUES 
(1, 'admin', '123456', '管理员', NULL, '13800000000', 0, 1, 1, NOW(), NOW());

-- 普通用户账号
INSERT INTO `user` VALUES 
(2, 'test1', '123456', '张三', NULL, '13800000001', 0, 0, 1, NOW(), NOW()),
(3, 'test2', '123456', '李四', NULL, '13800000002', 0, 0, 1, NOW(), NOW());
```

---

## 三、API 接口

### 3.1 账号密码登录

**接口**：`POST /api/user/auth/login`

**请求**：
```json
{
  "username": "admin",
  "password": "123456"
}
```

**响应**：
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
  }
}
```

**逻辑**：
1. 查询用户
2. 验证状态（未禁用）
3. 验证密码（明文）
4. 生成 JWT Token

---

### 3.2 手机号验证码登录

**接口**：`POST /api/user/auth/login-by-sms`

**请求**：
```json
{
  "phone": "13800138000",
  "code": "123456"
}
```

**响应**：
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 4,
      "username": "user_8000",
      "nickname": "用户 8000",
      "phone": "13800138000",
      "role": 0,
      "status": 1
    }
  }
}
```

**逻辑**：
1. 验证手机号和验证码
2. 验证验证码有效性
3. 查询或自动注册用户
4. 生成 Token

---

### 3.3 发送验证码

**接口**：`POST /api/user/auth/sms-code`

**请求**：
```json
{
  "phone": "13800138000"
}
```

**响应**：
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

**控制台输出**：
```
[SMS] 手机号：13800138000, 验证码：123456, 过期时间：2026-06-04 10:15:00
```

**逻辑**：
1. 验证手机号格式（11 位）
2. 检查发送频率（60 秒）
3. 生成 6 位验证码
4. 存储验证码（内存，5 分钟）
5. 打印到控制台

---

### 3.4 用户注册

**接口**：`POST /api/user/auth/register`

**请求**：
```json
{
  "username": "newuser",
  "password": "test123456",
  "nickname": "新用户",
  "phone": "13800138000"
}
```

**响应**：
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

**逻辑**：
1. 检查用户名是否存在
2. 明文存储密码
3. 创建用户

---

### 3.5 修改密码

**接口**：`POST /api/user/profile/update-password`

**权限**：需要登录

**请求**：
```json
{
  "oldPassword": "123456",
  "newPassword": "new123456"
}
```

**响应**：
```json
{
  "code": 200,
  "msg": "操作成功",
  "data": null
}
```

**逻辑**：

**首次设置密码**（密码为空）：
- 不验证旧密码
- 直接设置新密码

**修改密码**（已有密码）：
- 验证旧密码（明文）
- 验证新旧密码不同
- 更新为新密码

---

## 四、前端实现

### 4.1 登录页面

**路径**：`/login`

**功能**：
- Tab 切换（账号登录 / 手机号登录）
- 账号密码表单
- 手机号验证码表单
- 获取验证码（60 秒倒计时）
- 表单验证

**布局**：
```
┌────────────────────────────┐
│  用户登录                  │
├────────────────────────────┤
│  [账号登录] [手机号登录]   │
├────────────────────────────┤
│  用户名：[________]        │
│  密码：  [________]        │
│  [登录]                    │
│  还没有账号？立即注册      │
└────────────────────────────┘
```

---

### 4.2 个人中心

**路径**：`/profile`

**功能**：
- 查看个人信息
- 修改个人资料
- **修改密码**
- 管理收货地址

**修改密码弹窗**：
```
┌────────────────────────────┐
│  修改密码              [×] │
├────────────────────────────┤
│  原密码：[________]        │
│  新密码：[________]        │
│  确认密码：[________]      │
│                            │
│  [取消]      [保存]        │
└────────────────────────────┘
```

---

## 五、测试账号

### 管理员

| 用户名 | 密码 | 角色 | 手机号 |
|--------|------|------|--------|
| admin | 123456 | 管理员 | 13800000000 |

**登录地址**：http://localhost:3001/login

---

### 普通用户

| 用户名 | 密码 | 角色 | 手机号 |
|--------|------|------|--------|
| test1 | 123456 | 用户 | 13800000001 |
| test2 | 123456 | 用户 | 13800000002 |

**登录地址**：http://localhost:5173/login

---

## 六、测试用例

### 6.1 账号密码登录

**测试步骤**：
1. 访问登录页面
2. 输入用户名：`admin`
3. 输入密码：`123456`
4. 点击登录

**预期结果**：
- ✅ 登录成功
- ✅ 跳转到首页
- ✅ 保存 Token

---

### 6.2 手机号验证码登录

**测试步骤**：
1. 切换到"手机号登录"
2. 输入手机号：`13800138000`
3. 点击"获取验证码"
4. 查看后端控制台获取验证码
5. 输入验证码
6. 点击登录

**预期结果**：
- ✅ 验证码发送成功
- ✅ 60 秒倒计时
- ✅ 登录成功
- ✅ 新用户自动注册

---

### 6.3 修改密码

**首次设置密码**：
1. 手机号登录新用户
2. 进入个人中心
3. 点击"修改密码"
4. 输入新密码：`test123456`
5. 确认密码：`test123456`
6. 保存

**修改已有密码**：
1. 使用已设置密码的账号登录
2. 进入个人中心
3. 点击"修改密码"
4. 输入原密码：`123456`
5. 输入新密码：`new123456`
6. 确认新密码：`new123456`
7. 保存

**预期结果**：
- ✅ 密码修改成功
- ✅ 可使用新密码登录

---

## 七、常见问题

### Q1: 提示"密码错误"

**原因**：
- 数据库中的密码不是明文
- 密码输入错误

**解决**：
```sql
UPDATE `user` SET `password` = '123456' WHERE `username` = 'admin';
```

---

### Q2: 验证码收不到

**说明**：
- 开发环境验证码打印到后端控制台
- 查看后端日志输出

**日志示例**：
```
[SMS] 手机号：13800138000, 验证码：123456, 过期时间：2026-06-04 10:15:00
```

---

### Q3: 手机号登录提示"验证码错误"

**原因**：
- 验证码过期（>5 分钟）
- 验证码输入错误
- 验证码已使用

**解决**：
- 重新获取验证码
- 查看后端控制台确认验证码

---

## 八、安全说明

### 8.1 明文密码说明

**当前状态**：开发环境使用明文密码

**优点**：
- ✅ 实现简单
- ✅ 易于调试
- ✅ 性能更好

**缺点**：
- ❌ 数据库泄露会导致密码暴露
- ❌ 不适合生产环境

### 8.2 生产环境建议

如果部署到生产环境：

1. **使用 BCrypt 加密**：
   ```java
   PasswordEncoder encoder = new BCryptPasswordEncoder();
   String encoded = encoder.encode(password);
   ```

2. **升级现有密码**：
   ```sql
   -- 遍历所有用户，重新加密密码
   ```

3. **使用 HTTPS**：
   - 防止传输过程中密码泄露

4. **密码强度要求**：
   - 最少 8 位
   - 包含大小写字母、数字、特殊字符

---

## 九、技术栈

### 后端
- Spring Boot 2.7.18
- MyBatis-Plus
- JWT (jjwt)
- MySQL 8.0

### 前端
- Vue 3
- Vite
- Element Plus
- Axios

### 安全
- Spring Security
- JWT Token 认证
- 明文密码（开发环境）

---

## 十、文件清单

### 后端核心文件

| 文件 | 说明 |
|------|------|
| `UserServiceImpl.java` | 用户服务（登录、注册、修改密码） |
| `UserAuthController.java` | 用户认证接口 |
| `UserProfileController.java` | 用户信息接口 |
| `SecurityConfig.java` | 安全配置 |
| `SmsServiceImpl.java` | 短信服务（控制台打印） |

### 前端核心文件

| 文件 | 说明 |
|------|------|
| `Login.vue` | 登录页面 |
| `Profile.vue` | 个人中心 |
| `api/index.js` | API 接口 |
| `utils/request.js` | 请求封装 |

---

**文档更新日期**：2026-06-04

**适用环境**：开发环境

**重要提示**：明文密码仅用于开发和学习，生产环境必须使用加密存储！
