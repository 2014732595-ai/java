# 环境配置文档

## 项目配置信息总览

| 配置项 | 开发环境 | 生产环境 | 说明 |
|--------|----------|----------|------|
| 后端端口 | 8080 | 8080 | Spring Boot 服务端口 |
| 用户端前台端口 | 5173 | - | Vite 开发服务器 |
| 管理后台端口 | 5174 | - | Vite 开发服务器 |

---

## 一、数据库配置

### 1.1 数据库信息

```yaml
数据库类型：MySQL 8.0+
数据库名：secondhand_trade
数据库地址：localhost:3306
字符集：utf8mb4
```

### 1.2 数据库账号

```yaml
# 开发环境
username: root
password: 123456

# 生产环境（请修改为实际配置）
username: your_prod_username
password: your_prod_password
```

### 1.3 数据库连接 URL

```
jdbc:mysql://localhost:3306/secondhand_trade?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
```

### 1.4 初始化脚本

- 文件位置：`/init.sql`
- 执行方式：使用 MySQL 客户端或 Navicat 等工具执行

---

## 二、七牛云配置

### 2.1 七牛云账号信息

```yaml
# 七牛云控制台地址
https://portal.qiniu.com/

# 短信服务控制台
https://sms.console.qiniu.com/

# 对象存储服务控制台
https://portal.qiniu.com/kodo
```

### 2.2 密钥配置（已配置）

```yaml
# AccessKey
QINIU_ACCESS_KEY: AlJNdIXkVoQDAFPP5FAV16tbGJvzLAB6RYcrBxaN

# SecretKey
QINIU_SECRET_KEY: q6fmuyYNZEJiU94_6a8H4t2SLnwsgz8Wx-1jEguW
```

### 2.3 对象存储配置

```yaml
# 存储空间名称
QINIU_BUCKET: spring-b

# 存储空间区域
QINIU_REGION: 华南广东（hn-bkt）

# 外链域名
QINIU_DOMAIN: tfp0gq0km.hn-bkt.clouddn.com
```

### 2.4 短信配置

```yaml
# 短信签名 ID（需先在七牛云创建并审核通过）
QINIU_SIGN_ID: 待配置

# 短信模板 ID（需先在七牛云创建并审核通过）
QINIU_TEMPLATE_ID: 待配置
```

### 2.5 短信签名信息

```yaml
签名内容：【二手交易系统】
签名类型：企业/个人
状态：待创建/待审核
```

### 2.6 短信模板信息

```yaml
模板内容：您的验证码是：${code}，5 分钟内有效，请勿泄露给他人。
模板变量：code（6 位数字）
模板状态：待创建/待审核
```

---

## 三、配置文件位置

### 3.1 后端配置文件

**开发环境配置文件：**
- 路径：`back/src/main/resources/application.yml`
- 数据库配置位置：第 5-9 行
- 文件上传路径：第 33 行
- JWT 密钥：第 29 行

**生产环境配置方式：**

方式一：修改 application.yml
```yaml
spring:
  datasource:
    username: your_prod_username
    password: your_prod_password
```

方式二：使用环境变量（推荐）
```bash
export SPRING_DATASOURCE_USERNAME=your_prod_username
export SPRING_DATASOURCE_PASSWORD=your_prod_password
export QINIU_ACCESS_KEY=your_access_key
export QINIU_SECRET_KEY=your_secret_key
export QINIU_SIGN_ID=your_sign_id
export QINIU_TEMPLATE_ID=your_template_id
```

方式三：使用 application-prod.yml
```yaml
spring:
  datasource:
    url: jdbc:mysql://prod-host:3306/secondhand_trade?useSSL=true&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

qiniu:
  sms:
    access-key: ${QINIU_ACCESS_KEY}
    secret-key: ${QINIU_SECRET_KEY}
    sign-id: ${QINIU_SIGN_ID}
    template-id: ${QINIU_TEMPLATE_ID}
```

启动时激活：
```bash
java -jar secondhand-1.0.0.jar --spring.profiles.active=prod
```

### 3.2 前端配置文件

**用户端前台配置：**
- 路径：`forn/user-front/src/utils/request.js`
- API 基础地址：需要根据实际部署修改

**管理后台配置：**
- 路径：`forn/admin-front/src/utils/request.js`
- API 基础地址：需要根据实际部署修改

---

## 四、JWT 配置

```yaml
# JWT 密钥（生产环境请修改）
jwt:
  secret: secondhandTradeSecretKey20240101abcdef
  expiration: 86400000  # 24 小时（毫秒）
```

**生产环境建议：**
- 使用更复杂的密钥（至少 32 位随机字符串）
- 可通过环境变量配置：`JWT_SECRET=your_secret_key`

---

## 五、文件上传配置

```yaml
# 本地上传路径（开发环境）
file:
  upload-path: ./uploads/
  access-path: /api/file/**

# 生产环境建议配置到 OSS 或七牛云对象存储
```

---

## 六、环境变量配置模板

### 6.1 Windows 系统

创建 `.env` 文件或设置系统环境变量：

```powershell
# 数据库配置
set SPRING_DATASOURCE_USERNAME=root
set SPRING_DATASOURCE_PASSWORD=123456

# 七牛云配置
set QINIU_ACCESS_KEY=your_access_key
set QINIU_SECRET_KEY=your_secret_key
set QINIU_SIGN_ID=your_sign_id
set QINIU_TEMPLATE_ID=your_template_id

# JWT 配置
set JWT_SECRET=your_jwt_secret_key
```

### 6.2 Linux/Mac 系统

```bash
# ~/.bashrc 或 ~/.zshrc
export SPRING_DATASOURCE_USERNAME=root
export SPRING_DATASOURCE_PASSWORD=123456
export QINIU_ACCESS_KEY=your_access_key
export QINIU_SECRET_KEY=your_secret_key
export QINIU_SIGN_ID=your_sign_id
export QINIU_TEMPLATE_ID=your_template_id
export JWT_SECRET=your_jwt_secret_key
```

### 6.3 Docker 环境

```yaml
# docker-compose.yml
environment:
  - SPRING_DATASOURCE_USERNAME=root
  - SPRING_DATASOURCE_PASSWORD=123456
  - QINIU_ACCESS_KEY=AlJNdIXkVoQDAFPP5FAV16tbGJvzLAB6RYcrBxaN
  - QINIU_SECRET_KEY=q6fmuyYNZEJiU94_6a8H4t2SLnwsgz8Wx-1jEguW
  - QINIU_BUCKET=spring-b
  - QINIU_DOMAIN=tfp0gq0km.hn-bkt.clouddn.com
  - QINIU_SIGN_ID=待配置
  - QINIU_TEMPLATE_ID=待配置
```

---

## 七、配置检查清单

### 7.1 开发环境启动前检查

- [ ] MySQL 服务已启动
- [ ] 数据库 secondhand_trade 已创建
- [ ] init.sql 已执行
- [ ] application.yml 中数据库密码正确
- [ ] 端口 8080、5173、5174 未被占用

### 7.2 生产环境部署前检查

- [ ] 数据库密码已修改为强密码
- [ ] JWT 密钥已更换为随机字符串
- [ ] 七牛云密钥已从生产环境控制台获取
- [ ] 短信签名和模板已审核通过
- [ ] 环境变量已正确配置
- [ ] 敏感信息未提交到代码仓库

---

## 八、敏感信息安全建议

### 8.1 不要提交到 Git

确保 `.gitignore` 包含：

```gitignore
# 配置文件
application-prod.yml
application-local.yml

# 环境文件
.env
.env.local
.env.production

# 密钥文件
*.key
*.pem
```

### 8.2 使用配置中心（可选）

生产环境建议使用配置中心管理敏感配置：
- Nacos
- Apollo
- Spring Cloud Config

### 8.3 密钥轮换

定期更换以下密钥：
- 数据库密码（建议 90 天）
- JWT 密钥（建议 180 天）
- 七牛云 AccessKey/SecretKey（建议 180 天）

---

## 九、配置记录表

### 9.1 当前配置记录

| 配置项 | 值 | 更新日期 | 备注 |
|--------|-----|----------|------|
| 数据库密码 | 123456 | 2026-06-04 | 开发环境 |
| JWT 密钥 | secondhandTradeSecretKey20240101abcdef | 2026-06-04 | 开发环境 |
| 七牛 AccessKey | 待配置 | - | - |
| 七牛 SecretKey | 待配置 | - | - |
| 七牛 Sign ID | 待配置 | - | - |
| 七牛 Template ID | 待配置 | - | - |

### 9.2 配置更新日志

| 日期 | 配置项 | 修改内容 | 修改人 |
|------|--------|----------|--------|
| 2026-06-04 | 初始配置 | 创建配置文档 | - |

---

## 十、常见问题

### Q1: 数据库连接失败
```
检查 MySQL 服务是否启动
检查数据库密码是否正确
检查端口 3306 是否开放
```

### Q2: 短信发送失败
```
检查七牛云账户是否实名认证
检查账户余额是否充足
检查签名和模板是否审核通过
检查 AccessKey 和 SecretKey 是否正确
```

### Q3: 文件上传失败
```
检查 uploads 目录是否有写权限
检查磁盘空间是否充足
检查文件大小限制配置
```

---

**文档更新日期：2026-06-04**

**注意：请将此文档添加到 `.gitignore`，不要提交到代码仓库！**
