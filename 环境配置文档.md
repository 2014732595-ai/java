# 环境配置文档

## 快速配置指南

### 一、端口配置

| 服务 | 端口 | 访问地址 |
|------|------|----------|
| 后端 | 8080 | http://localhost:8080 |
| 用户端前台 | 3000 | http://localhost:3000 |
| 管理后台 | 3001 | http://localhost:3001 |

---

### 二、数据库配置

**配置文件**：`back/src/main/resources/application.yml`

```yaml
数据库：MySQL 8.0+
数据库名：secondhand_trade
地址：localhost:3306
账号：root
密码：123456
```

**初始化**：
```bash
mysql -u root -p < init.sql
```

---

### 三、验证码配置

**当前实现**：开发环境使用控制台打印验证码

**配置位置**：`back/src/main/resources/application.yml`

```yaml
sms:
  code:
    length: 6          # 验证码长度
    expire-minutes: 5  # 有效期 5 分钟
    send-interval: 60  # 发送间隔 60 秒
```

**使用方式**：
1. 前端发送验证码请求
2. 后端控制台打印验证码
3. 查看后端日志获取验证码

**日志示例**：
```
[SMS] 手机号：13800138000, 验证码：123456, 过期时间：2026-06-04 10:15:00
```

---

### 四、JWT 配置

**配置文件**：`back/src/main/resources/application.yml`

```yaml
jwt:
  secret: secondhandTradeSecretKey20240101abcdef
  expiration: 86400000  # 24 小时
```

---

### 五、文件上传配置

**配置文件**：`back/src/main/resources/application.yml`

```yaml
file:
  upload-path: ./uploads/
```

**上传目录**：项目根目录下的 `uploads` 文件夹

---

### 六、前端 API 配置

**用户端前台**：`forn/user-front/src/utils/request.js`
- baseURL: `/api`
- 通过 Vite 代理到后端

**管理后台**：`forn/admin-front/src/utils/request.js`
- baseURL: `/api`
- 通过 Vite 代理到后端

---

### 七、启动命令

**后端**：
```bash
cd back
mvn spring-boot:run
```

**用户端前台**：
```bash
cd forn/user-front
npm install
npm run dev
```

**管理后台**：
```bash
cd forn/admin-front
npm install
npm run dev
```

---

### 八、测试账号

**管理员**：
- 用户名：admin
- 密码：123456
- 角色：管理员
- 登录地址：http://localhost:3001/login

**普通用户**：
- 用户名：test1 / test2
- 密码：123456
- 角色：用户
- 登录地址：http://localhost:3000/login

**手机号登录**：
- 任意 11 位数字
- 验证码查看后端控制台

---

### 九、检查清单

启动前检查：
- [ ] MySQL 服务已启动
- [ ] 数据库已初始化（执行 init.sql）
- [ ] 端口 8080、3000、3001 未被占用
- [ ] Node.js 已安装（前端）
- [ ] Maven 已安装（后端）

---

### 十、常见问题

**Q1: 数据库连接失败**
```
解决：检查 MySQL 服务、数据库密码、端口 3306
```

**Q2: 验证码收不到**
```
解决：查看后端控制台日志输出
```

**Q3: 端口被占用**
```
解决：修改配置文件中的端口号
```

---

**最后更新**：2026-06-04
**适用环境**：开发环境
