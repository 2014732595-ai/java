# 前端功能总结

## 版本信息

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.0 | 2026-06-04 | 初始版本 |
| v1.1 | 2026-06-04 | 支持明文密码 |

---

## 一、核心功能

### 1.1 登录功能

**文件**：`forn/user-front/src/views/Login.vue`

**功能特性**：
- ✅ 账号密码登录
- ✅ 手机号验证码登录
- ✅ Tab 切换登录方式
- ✅ 表单验证
- ✅ 60 秒倒计时

**API 调用**：
```javascript
// 账号密码登录
login({ username, password })

// 手机号登录
loginBySms({ phone, code })

// 发送验证码
sendSmsCode({ phone })
```

**验证规则**：
- 用户名：必填
- 密码：必填
- 手机号：必填 + 11 位数字
- 验证码：必填 + 6 位数字

---

### 1.2 个人中心

**文件**：`forn/user-front/src/views/Profile.vue`

**功能特性**：
- ✅ 查看个人信息
- ✅ 修改个人资料
- ✅ 修改密码
- ✅ 管理收货地址

**修改密码**：
- 首次设置密码（密码为空）
- 修改已有密码
- 密码格式验证（6-20 位数字或字母）
- 两次密码一致性验证

---

### 1.3 API 接口

**文件**：`forn/user-front/src/api/index.js`

**认证接口**：
```javascript
login(data)              // 账号密码登录
loginBySms(data)         // 手机号验证码登录
sendSmsCode(data)        // 发送验证码
register(data)           // 用户注册
updatePassword(data)     // 修改密码
getUserInfo()            // 获取个人信息
updateProfile(data)      // 更新个人信息
```

**商品接口**：
```javascript
getProducts(params)      // 商品列表
getProductDetail(id)     // 商品详情
createProduct(data)      // 发布商品
updateProduct(id, data)  // 编辑商品
deleteProduct(id)        // 下架商品
getMyProducts(params)    // 我的商品
```

**订单接口**：
```javascript
createOrder(data)        // 创建订单
getBuyOrders(params)     // 购买订单
getSellOrders(params)    // 卖出订单
updateOrderStatus(id, status)  // 更新订单状态
```

**地址接口**：
```javascript
getAddressList()         // 地址列表
createAddress(data)      // 创建地址
updateAddress(id, data)  // 更新地址
deleteAddress(id)        // 删除地址
getDefaultAddress()      // 默认地址
```

**其他接口**：
```javascript
getCategories()          // 分类列表
getComments(productId)   // 留言列表
createComment(data)      // 发布留言
uploadFile(file)         // 文件上传
```

---

## 二、组件结构

### 2.1 页面组件

```
src/views/
├── Login.vue          # 登录页面
├── Profile.vue        # 个人中心
├── Register.vue       # 注册页面（如有）
├── Products.vue       # 商品列表
├── ProductDetail.vue  # 商品详情
├── Orders.vue         # 订单管理
└── Address.vue        # 地址管理
```

### 2.2 组件文件

```
src/components/
├── Header.vue         # 页面头部
├── Footer.vue         # 页面底部
└── ...
```

### 2.3 工具类

```
src/utils/
├── request.js         # Axios 请求封装
└── storage.js         # LocalStorage 封装
```

### 2.4 状态管理

```
src/stores/
├── user.js            # 用户状态
└── ...
```

---

## 三、请求封装

**文件**：`src/utils/request.js`

**核心功能**：
- ✅ 基础 URL 配置
- ✅ Token 自动携带
- ✅ 响应拦截器
- ✅ 错误处理
- ✅ 401 自动跳转登录

**请求配置**：
```javascript
const request = axios.create({
  baseURL: 'http://localhost:8080/api',
  timeout: 5000
})

// 请求拦截器
request.interceptors.request.use(
  config => {
    const token = localStorage.getItem('token')
    if (token) {
      config.headers.Authorization = `Bearer ${token}`
    }
    return config
  }
)

// 响应拦截器
request.interceptors.response.use(
  response => {
    const res = response.data
    if (res.code !== 200) {
      return Promise.reject(new Error(res.msg || 'Error'))
    }
    return res.data
  }
)
```

---

## 四、状态管理

**文件**：`src/stores/user.js`

**用户状态**：
```javascript
import { defineStore } from 'pinia'

export const useUserStore = defineStore('user', {
  state: () => ({
    token: localStorage.getItem('token') || '',
    userInfo: JSON.parse(localStorage.getItem('userInfo') || '{}')
  }),
  
  actions: {
    setToken(token) {
      this.token = token
      localStorage.setItem('token', token)
    },
    setInfo(info) {
      this.userInfo = info
      localStorage.setItem('userInfo', JSON.stringify(info))
    },
    logout() {
      this.token = ''
      this.userInfo = {}
      localStorage.removeItem('token')
      localStorage.removeItem('userInfo')
    }
  }
})
```

---

## 五、路由配置

**文件**：`src/router/index.js`

**路由守卫**：
```javascript
router.beforeEach((to, from, next) => {
  const token = localStorage.getItem('token')
  
  if (to.meta.requiresAuth && !token) {
    next('/login')
  } else {
    next()
  }
})
```

**路由示例**：
```javascript
const routes = [
  {
    path: '/login',
    name: 'Login',
    component: Login
  },
  {
    path: '/profile',
    name: 'Profile',
    component: Profile,
    meta: { requiresAuth: true }
  }
]
```

---

## 六、表单验证

### 6.1 登录表单

```javascript
const accountRules = {
  username: [{ required: true, message: '请输入用户名', trigger: 'blur' }],
  password: [{ required: true, message: '请输入密码', trigger: 'blur' }]
}
```

### 6.2 手机号登录表单

```javascript
const smsRules = {
  phone: [
    { required: true, message: '请输入手机号', trigger: 'blur' },
    { pattern: /^\d{11}$/, message: '手机号格式不正确', trigger: 'blur' }
  ],
  code: [
    { required: true, message: '请输入验证码', trigger: 'blur' },
    { pattern: /^\d{6}$/, message: '验证码格式不正确', trigger: 'blur' }
  ]
}
```

### 6.3 密码验证规则

```javascript
const validateNewPassword = (rule, value, callback) => {
  if (!value) {
    callback(new Error('请输入新密码'))
  } else if (!/^[a-zA-Z0-9]{6,20}$/.test(value)) {
    callback(new Error('密码格式不正确，长度为 6-20 位数字或字母'))
  } else {
    callback()
  }
}

const validateConfirmPassword = (rule, value, callback) => {
  if (!value) {
    callback(new Error('请再次输入新密码'))
  } else if (value !== passwordForm.newPassword) {
    callback(new Error('两次输入的密码不一致'))
  } else {
    callback()
  }
}
```

---

## 七、UI 组件库

**使用**：Element Plus

**主要组件**：
- `el-card` - 卡片容器
- `el-form` - 表单
- `el-input` - 输入框
- `el-button` - 按钮
- `el-tabs` - 标签页切换
- `el-dialog` - 对话框
- `el-table` - 表格
- `el-tag` - 标签
- `el-switch` - 开关
- `el-alert` - 提示框

---

## 八、开发规范

### 8.1 代码风格

- 使用 Composition API (`<script setup>`)
- 使用响应式 API (`ref`, `reactive`)
- 使用 Pinia 进行状态管理
- 使用 Axios 进行 HTTP 请求

### 8.2 命名规范

- 组件名：PascalCase (如 `Login.vue`)
- 变量名：camelCase (如 `accountForm`)
- 常量名：UPPER_SNAKE_CASE (如 `API_BASE_URL`)
- 方法名：camelCase (如 `handleLogin`)

### 8.3 注释规范

```javascript
/**
 * 登录处理函数
 * @param {Object} form - 登录表单数据
 * @returns {Promise<void>}
 */
const handleLogin = async (form) => {
  // 实现逻辑
}
```

---

## 九、构建配置

**文件**：`vite.config.js`

**主要配置**：
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'

export default defineConfig({
  plugins: [vue()],
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8080',
        changeOrigin: true
      }
    }
  }
})
```

---

## 十、环境变量

**文件**：`.env` / `.env.production`

```env
# 开发环境
VITE_API_BASE_URL=http://localhost:8080/api

# 生产环境
VITE_API_BASE_URL=https://api.example.com
```

---

## 十一、文件清单

### 核心文件

| 文件 | 说明 | 路径 |
|------|------|------|
| `Login.vue` | 登录页面 | `src/views/` |
| `Profile.vue` | 个人中心 | `src/views/` |
| `api/index.js` | API 接口 | `src/api/` |
| `request.js` | 请求封装 | `src/utils/` |
| `user.js` | 用户状态 | `src/stores/` |
| `index.js` | 路由配置 | `src/router/` |

### 配置文件

| 文件 | 说明 |
|------|------|
| `package.json` | 项目依赖 |
| `vite.config.js` | Vite 配置 |
| `.eslintrc.js` | ESLint 配置 |
| `index.html` | 入口 HTML |

---

## 十二、启动说明

### 12.1 安装依赖

```bash
npm install
```

### 12.2 启动开发服务器

```bash
npm run dev
```

### 12.3 构建生产版本

```bash
npm run build
```

### 12.4 预览生产构建

```bash
npm run preview
```

---

## 十三、常见问题

### Q1: 跨域问题

**解决**：在 `vite.config.js` 配置代理

```javascript
proxy: {
  '/api': {
    target: 'http://localhost:8080',
    changeOrigin: true
  }
}
```

### Q2: Token 失效

**解决**：
1. 检查响应拦截器的 401 处理
2. 清除本地存储的 token
3. 跳转到登录页

### Q3: 状态管理不更新

**解决**：
1. 使用 Pinia 的 `storeToRefs` 解构
2. 不要直接修改 state，使用 actions

---

**文档更新日期**：2026-06-04

**适用环境**：开发环境
