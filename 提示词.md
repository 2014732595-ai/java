# 二手线上交易系统 - AI 提示词

## 项目概述
基于 Spring Boot 的二手线上交易平台，前后端完全分离。系统分为**用户端**和**管理端**两个独立前端项目，共用同一后端 API。

## 技术栈
- **前端(用户端)**: Vue 3 + Vite + Vue Router + Pinia + Element Plus + Axios
- **前端(管理端)**: Vue 3 + Vite + Vue Router + Pinia + Element Plus + Axios
- **后端**: Spring Boot 2.7.x + JDK 8 + MyBatis-Plus + Spring Security + JWT
- **数据库**: MySQL 8
- **构建工具**: Maven (后端) / npm (前端)

## 项目结构
```
项目根目录/
├── forn/                      # 前端代码 (Vue 3)
│   ├── user-front/            # 用户端 (买家/卖家)
│   │   ├── src/
│   │   │   ├── api/           # 接口请求
│   │   │   ├── assets/        # 静态资源
│   │   │   ├── components/    # 公共组件
│   │   │   ├── router/        # 路由配置
│   │   │   ├── stores/        # Pinia 状态管理
│   │   │   ├── views/         # 页面组件
│   │   │   └── utils/         # 工具函数
│   │   ├── package.json
│   │   └── vite.config.js
│   │
│   └── admin-front/           # 管理端 (后台管理)
│       ├── src/
│       │   ├── api/
│       │   ├── assets/
│       │   ├── components/
│       │   ├── router/
│       │   ├── stores/
│       │   ├── views/
│       │   └── utils/
│       ├── package.json
│       └── vite.config.js
│
└── back/                      # 后端代码 (Spring Boot)
    ├── src/main/java/com/example/secondhand/
    │   ├── config/             # 配置类
    │   ├── controller/
    │   │   ├── user/           # 用户端接口
    │   │   └── admin/          # 管理端接口
    │   ├── service/            # 业务层
    │   ├── mapper/             # 数据访问层
    │   ├── entity/             # 实体类
    │   ├── dto/                # 数据传输对象
    │   ├── common/             # 公共类(结果封装/异常)
    │   └── security/           # 安全配置(JWT/权限)
    ├── src/main/resources/
    │   ├── application.yml
    │   └── mapper/             # MyBatis XML
    └── pom.xml
```

---

## 核心功能模块

### 用户端功能

#### 1. 用户模块
- 注册/登录 (JWT 认证)
- 个人信息管理 (头像、昵称、联系方式)
- 收货地址管理

#### 2. 商品模块
- 浏览商品 (首页推荐、搜索、分类筛选、分页)
- 商品详情 (图片轮播、卖家信息、留言列表)
- 发布商品 (标题、描述、多图上传、价格、分类、成色)
- 编辑/下架我的商品

#### 3. 交易模块
- 下单购买
- 我的订单 (买家视角: 待付款/待发货/待收货/已完成)
- 我的订单 (卖家视角: 待发货/已发货/已完成)
- 订单状态流转 (待付款 → 已付款 → 已发货 → 已完成)
- 取消订单

#### 4. 留言/评价模块
- 商品留言/回复
- 交易评价

### 管理端功能

#### 1. 管理员模块
- 管理员登录 (独立登录入口)
- 管理员信息管理

#### 2. 用户管理
- 用户列表 (分页、搜索)
- 查看用户详情
- 禁用/启用用户

#### 3. 商品管理
- 商品列表 (全部商品、分页、搜索)
- 强制下架违规商品
- 商品分类管理 (增删改查)

#### 4. 订单管理
- 全部订单列表
- 订单详情查看
- 订单统计 (数量/金额)

#### 5. 数据统计
- 用户数/商品数/订单数概览
- 交易趋势图
- 热门分类统计

#### 6. 留言管理
- 留言列表
- 删除违规留言

---

## 数据库设计

### user 用户表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT (PK) | 主键 |
| username | VARCHAR(50) | 用户名 |
| password | VARCHAR(100) | 密码 (BCrypt 加密) |
| nickname | VARCHAR(50) | 昵称 |
| avatar | VARCHAR(255) | 头像 |
| phone | VARCHAR(20) | 手机号 |
| role | TINYINT | 角色 (0-普通用户 1-管理员) |
| status | TINYINT | 状态 (0-禁用 1-正常) |
| create_time | DATETIME | 创建时间 |

### product 商品表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT (PK) | 主键 |
| title | VARCHAR(100) | 标题 |
| description | TEXT | 描述 |
| price | DECIMAL(10,2) | 价格 |
| original_price | DECIMAL(10,2) | 原价 |
| category_id | BIGINT | 分类ID |
| condition_level | TINYINT | 成色 (1-5) |
| images | TEXT | 图片URL (逗号分隔) |
| seller_id | BIGINT | 卖家ID |
| status | TINYINT | 状态 (0-下架 1-在售 2-管理员下架) |
| create_time | DATETIME | 发布时间 |

### orders 订单表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT (PK) | 主键 |
| order_no | VARCHAR(32) | 订单号 |
| buyer_id | BIGINT | 买家ID |
| seller_id | BIGINT | 卖家ID |
| product_id | BIGINT | 商品ID |
| amount | DECIMAL(10,2) | 金额 |
| status | TINYINT | 状态 (0-待付款 1-已付款 2-已发货 3-已完成 4-已取消) |
| address | VARCHAR(255) | 收货地址 |
| create_time | DATETIME | 下单时间 |

### category 分类表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT (PK) | 主键 |
| name | VARCHAR(50) | 分类名称 |
| sort | INT | 排序 |

### comment 留言表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT (PK) | 主键 |
| product_id | BIGINT | 商品ID |
| user_id | BIGINT | 留言用户ID |
| content | VARCHAR(500) | 留言内容 |
| create_time | DATETIME | 留言时间 |

### address 收货地址表
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT (PK) | 主键 |
| user_id | BIGINT | 用户ID |
| receiver | VARCHAR(50) | 收货人 |
| phone | VARCHAR(20) | 联系电话 |
| detail | VARCHAR(255) | 详细地址 |
| is_default | TINYINT | 是否默认 (0-否 1-是) |

---

## 后端提示词

```
请使用 Spring Boot 2.7.x + JDK 8 构建后端项目，前后端完全分离:

### 技术要求
1. 使用 MyBatis-Plus 作为 ORM 框架
2. 使用 Spring Security + JWT 实现认证授权，基于角色 (ROLE_USER / ROLE_ADMIN) 区分权限
3. 统一返回格式: {code: 200, message: "success", data: {}}
4. 全局异常处理 (@ControllerAdvice)
5. 分页使用 MyBatis-Plus 内置 Page
6. 文件上传使用本地存储 (配置访问路径)
7. 接口遵循 RESTful 风格
8. 使用 @CrossOrigin 或配置类解决跨域问题
9. 使用 application.yml 配置数据库、JWT 密钥、文件上传路径等

### 接口设计 (用户端 /api/user/*)
认证:
- POST /api/user/auth/login - 用户登录
- POST /api/user/auth/register - 用户注册

商品:
- GET /api/user/products - 商品列表 (分页+搜索+分类)
- GET /api/user/products/{id} - 商品详情
- POST /api/user/products - 发布商品 (需登录)
- PUT /api/user/products/{id} - 编辑商品 (仅卖家)
- DELETE /api/user/products/{id} - 下架商品 (仅卖家)

订单:
- POST /api/user/orders - 创建订单 (需登录)
- GET /api/user/orders/buy - 我买的订单
- GET /api/user/orders/sell - 我卖的订单
- PUT /api/user/orders/{id}/status - 更新订单状态

其他:
- GET /api/user/categories - 分类列表
- GET/POST/PUT/DELETE /api/user/address/* - 地址管理
- GET/POST /api/user/comments/* - 留言管理
- GET/PUT /api/user/profile - 个人信息

### 接口设计 (管理端 /api/admin/*)
认证:
- POST /api/admin/auth/login - 管理员登录

管理:
- GET /api/admin/users - 用户列表
- PUT /api/admin/users/{id}/status - 禁用/启用用户
- GET /api/admin/products - 商品列表
- PUT /api/admin/products/{id}/status - 强制下架
- GET/POST/PUT/DELETE /api/admin/categories - 分类管理
- GET /api/admin/orders - 订单列表
- GET /api/admin/stats - 数据统计
- GET/DELETE /api/admin/comments - 留言管理

### 安全要求
- 管理员接口仅限 ROLE_ADMIN 访问
- 用户操作接口仅限 ROLE_USER 访问
- 商品编辑/下架需校验卖家身份
- 密码 BCrypt 加密存储
```

---

## 前端提示词 (用户端)

```
请使用 Vue 3 + Vite + Element Plus 构建用户端前端项目:

### 技术要求
1. 使用 Vue Router 实现路由管理
2. 使用 Pinia 管理全局状态 (用户信息、Token)
3. 使用 Axios 封装请求拦截器 (自动携带 Token、统一错误处理)
4. 路由守卫: 未登录跳转登录页，已登录访问登录页跳转首页
5. 组件使用 Composition API (script setup 语法糖)
6. 响应式布局，适配 PC 和移动端

### 页面路由
- /login - 用户登录
- /register - 用户注册
- / - 首页 (商品列表、搜索、分类导航)
- /product/:id - 商品详情 (图片轮播、留言、联系卖家)
- /publish - 发布商品
- /my-products - 我的商品 (在售/已下架)
- /my-orders - 我的订单 (买家/卖家)
- /profile - 个人中心 (信息修改、地址管理)

### 组件
- Header - 顶部导航栏
- ProductCard - 商品卡片
- SearchBar - 搜索组件
- ImageUpload - 图片上传组件
- OrderStatus - 订单状态标签
```

---

## 前端提示词 (管理端)

```
请使用 Vue 3 + Vite + Element Plus 构建管理端前端项目:

### 技术要求
1. 使用 Vue Router 实现路由管理
2. 使用 Pinia 管理全局状态 (管理员信息、Token)
3. 使用 Axios 封装请求 (管理端独立 baseURL)
4. 路由守卫: 未登录跳转登录页，非管理员禁止访问
5. 组件使用 Composition API (script setup 语法糖)
6. 后台管理布局: 侧边栏 + 顶部 + 内容区

### 页面路由
- /login - 管理员登录
- /dashboard - 数据概览 (统计卡片、图表)
- /users - 用户管理 (列表、搜索、禁用)
- /products - 商品管理 (列表、搜索、强制下架)
- /categories - 分类管理 (增删改查)
- /orders - 订单管理 (列表、详情)
- /comments - 留言管理 (列表、删除)

### 布局组件
- AdminLayout - 后台布局 (侧边栏 + Header + Content)
- Sidebar - 侧边栏菜单
- HeaderBar - 顶部栏 (面包屑、退出)
```

---

## 开发要求
1. 代码规范: 遵循阿里巴巴 Java 开发手册
2. 接口文档: 使用 Swagger/Knife4j 生成 API 文档
3. 前端 UI: 使用 Element Plus 组件库
4. 图片处理: 支持多图上传、预览、压缩
5. 安全性: 密码 BCrypt 加密、SQL 注入防护、XSS 防护、接口权限校验
6. 日志: 使用 Slf4j 记录关键操作日志
7. 前后端部署: 分别独立部署，通过跨域或 Nginx 反向代理对接
8. 环境变量: 前端使用 .env 文件管理不同环境配置
